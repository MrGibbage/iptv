node_modules/
dist/
dist-electron/
dist-ssr/
out/
release/
.env
*.log

# Sample Xtream data — contains real account URL/credentials, never commit
playlist.m3u
sonix.xml

# libmpv runtime — large third-party binary, not source; see README for setup
libmpv-2.dll

